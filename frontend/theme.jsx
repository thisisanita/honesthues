import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#CA7DF9",
    },
    secondary: {
      main: "#775651",
    },
  },
});

export default theme;
